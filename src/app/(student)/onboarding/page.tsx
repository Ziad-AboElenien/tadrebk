import StudentOnboardingScreen from '@/features/student/screens/student-onboarding.screen';

export const metadata = {
  title: 'Student Onboarding | Tadrebk',
};

export default function StudentOnboardingPage() {
  return <StudentOnboardingScreen />;
}
